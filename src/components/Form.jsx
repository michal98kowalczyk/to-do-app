import React from "react";

import "../styles/Form.css";

const Form = () => {
    return(
        <form >
            <input type="text"/>
            <input type="date"/>
            <input type="checkbox"/>

            <button>Dodaj</button>
        </form>
    )
}

export default Form;